import numpy as np

arr = np.array([3, 2, 0, 1])
print(np.sort(arr))

# string sorting
arrString = np.array(['banana', 'cherry', 'apple'])
print(np.sort(arrString))

# boolean sorting
arrBoolean = np.array([True, False, True])
print(np.sort(arrBoolean))
